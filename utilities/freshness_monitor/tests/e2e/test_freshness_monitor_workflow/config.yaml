test_workflow_args:
  workflow_id: "freshness-monitor-test"
  threshold_days: 30
  connection:
    connection_name: "freshness-monitor-test"
    connection_qualified_name: "default/freshness-monitor/test"
test_name: "test_freshness_monitor_workflow"
server_config:
  server_host: "http://localhost:8000"
  server_version: "workflows/v1"
expected_api_responses:
  workflow:
    success: true
    message: "Workflow started successfully"

